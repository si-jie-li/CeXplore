# C. elegans Lineage Explorer Lite：开发与实现说明

本文面向“会用 Python 做数据分析，但不熟悉网页开发、npm、React、TypeScript”的读者。目标不是让你立刻成为前端工程师，而是让你能看懂这个工具由什么组成、各部分为什么存在、数据如何从文件流到谱系树和三维胚胎，以及以后应该到哪里修改功能。

---

## 1. 先用一句话理解这个工具

这是一个只在本机浏览器中运行的单页应用（single-page application）：浏览器读取一个或多个胚胎坐标表，在内存中建立 embryo/time/trajectory 索引和细胞谱系，然后用 WebGL 绘制单胚胎、多胚胎叠加或平均核位置，用 SVG 绘制谱系树，并由一个共享状态把所有视图、颜色和分组同步起来。

它没有服务器端数据库，也不会把上传的数据自动发送到网络。

可以把它类比成一个 Python 程序：

```text
CSV/TSV/XLSX
    ↓
pandas.read_csv() / read_excel()       ← 对应本项目的 loaders
    ↓
清洗、groupby(frame)、建立索引          ← 对应 frameIndex
    ↓
谱系关系表 + 递归查找                   ← 对应 lineage
    ↓
一个保存当前选择、颜色、时间点的对象     ← 对应 Zustand store
    ↓
matplotlib/plotly/napari 的多个联动视图  ← 对应 React + SVG + Three.js
```

区别在于：Python 脚本通常从头运行到尾，而浏览器应用一直运行，用户每点击一次或移动一次时间轴，只有受影响的界面部分被更新。

---

## 2. npm、Node.js、nvm、Vite 分别是什么

### 2.1 JavaScript 与 Node.js

JavaScript 原本主要运行在浏览器中。Node.js 是一个让 JavaScript 也能在终端中运行的环境。

Python 类比：

| 网页生态 | Python 类比 | 作用 |
|---|---|---|
| JavaScript/TypeScript | Python 语言 | 编写程序 |
| Node.js | Python 解释器 | 在终端运行构建工具 |
| `node --version` | `python --version` | 查看解释器版本 |

注意：最终打开工具时，三维视图实际运行在浏览器里；Node.js 主要负责开发阶段的依赖安装、代码检查和启动本地网页服务。

### 2.2 npm

npm 是 Node.js 生态的包管理器，相当于 Python 中的 `pip`，但同时也常用来运行项目命令。

```bash
npm install
```

类似：

```bash
pip install -r requirements.txt
```

它会读取 `package.json` 中声明的依赖，把所需包安装到项目的 `node_modules/` 目录，并按照 `package-lock.json` 锁定的版本保证可重复安装。

```bash
npm run dev
```

类似执行一个在 `pyproject.toml` 或 Makefile 中定义的快捷命令。这里它会启动 Vite 开发服务器。

### 2.3 nvm

nvm（Node Version Manager）用于安装和切换多个 Node.js 版本，可以类比为 `pyenv`。

项目中的 `.nvmrc` 写着 `22.14.0`，表示这个项目验证过的 Node.js 版本。执行：

```bash
nvm use
```

nvm 会读取 `.nvmrc` 并切换到对应版本。这不会改变项目的数据。

### 2.4 Vite

Vite 是开发服务器和生产构建工具，可以粗略类比为：

- 开发时的 Flask/Django 本地服务，加上自动刷新；
- 同时负责把 TypeScript、React、CSS 和资源打包成浏览器可读取的 HTML/JavaScript/CSS。

开发模式：

```bash
npm run dev
```

代码修改后，浏览器可快速刷新。

生产构建：

```bash
npm run build
```

Vite 会生成 `dist/`，里面是已经压缩打包的静态网页文件。

---

## 3. 为什么使用 React 和 TypeScript

### 3.1 React：用组件组织交互界面

React 把界面拆成小组件。例如：

- `Embryo3D` 只负责三维胚胎；
- `LineageTree` 只负责谱系树；
- `CellList` 只负责细胞列表；
- `PlaybackControls` 只负责播放。

组件更接近 Python 中“一个职责明确的类或函数”，而不是把所有代码写在一个大脚本中。

React 的核心思想是声明式渲染：程序描述“当前状态下界面应该是什么样”，状态变化后 React 计算需要更新的部分。例如：

```text
currentFrameIndex 从 84 变成 85
    ↓
读取 frame 85 的 observations
    ↓
更新三维实例矩阵、时间轴文字和谱系状态
```

不需要手动清除整个页面再重画。

### 3.2 TypeScript：带类型检查的 JavaScript

TypeScript 可以类比为使用 `mypy`、`dataclass` 和类型注解的 Python。它在构建前检查：

- 一个 observation 是否有 `cellId/x/y/z/step`；
- 一个函数需要字符串还是数字；
- 可选字段是否被错误地当作必填字段；
- 组件之间传递的数据形状是否一致。

浏览器最终执行的仍然是 JavaScript。TypeScript 的作用主要是提前发现错误，提高后续修改的可靠性。

---

## 4. 整体架构与数据流

```text
用户一次选择一个或多个文件
    ↓
检查文件头和少量样例行（inspectFile）
    ↓
每个文件依次确认列映射；扫描 embryo/sample column 的全部 ID
    ↓
用户勾选每个文件要导入的 embryo；流式解析并合并（loadMappedRows）
    ↓
清洗、去重、坐标归一化、建立 frame/trajectory 索引
    ↓
canonical parent table + 用户 parent 列
    ↓
生成 LineageModel 和静态树布局
    ↓
存入一个共享 Zustand store
    ↓
┌───────────┬─────────────┬──────────┬────────────┐
│ 三维胚胎   │ 谱系树       │ 细胞列表  │ Cell Groups│
└───────────┴─────────────┴──────────┴────────────┘
              全部读取同一个状态
```

共享状态是同步的关键。项目没有为三维视图和谱系树分别保存一份 selection；它们都读取 `state.selection`。

因此：

```text
在谱系树点击 ABpl
    ↓
store.selection = {ABpl}
    ↓
三维视图、谱系树、列表同时读到这个集合
    ↓
三处同时高亮
```

如果选择“Lineage”，则先通过 `getDescendants(ABpl)` 得到数据中实际存在的所有后代，再一次性写入同一个 selection 集合。

---

## 5. 文件读取和列映射的实现

### 5.1 为什么先检查文件、再真正读取

用户选中文件后，程序先读取文件开头的一小段，识别：

- 分隔符是逗号还是 Tab；
- 有哪些列；
- 前几十行的示例值；
- 是否可能有 `cell_name`、`time`、`A_pos` 等常见字段。

然后显示 Column Mapper。这样用户可以在完整解析之前纠正映射。

对于当前研究数据，自动建议为：

```text
Cell ID       → cell_name
AP coordinate → A_pos
LR coordinate → L_pos
VD coordinate → D_pos
Time          → time
Embryo column → embryo_id
Embryo IDs    → 扫描后显示所有 ID，用 checkbox 勾选一个或多个
```

### 5.2 CSV/TSV 为什么采用流式解析

`01_wt_truncated_axis_aligned.tsv` 有约 233 万行。如果像 `pandas.read_csv()` 一样一次性把所有列和所有胚胎放入浏览器内存，会浪费大量内存。

这里使用 Papa Parse 的分块/Worker 方式：

1. 先流式扫描 `embryo_id` 的不同取值并显示 checkbox；
2. 用户勾选要导入的 ID；
3. 文件再次按块解析，不匹配所选 embryo 的行立即丢弃；
4. 只保存需要的 ID、时间、坐标和可选 parent；
5. 解析工作放到 Web Worker，减少主界面卡顿。

它类似 Python 中：

```python
selected = {"ctr_emb1", "ctr_emb2"}
for chunk in pandas.read_csv(path, chunksize=100_000, sep="\t"):
    keep = chunk[chunk["embryo_id"].isin(selected)]
    retained.append(keep[needed_columns])
```

### 5.3 XLSX 为什么单独处理

浏览器中的 XLSX 解析器通常需要先读取整个工作簿。程序会：

1. 读取工作簿；
2. 列出所有 worksheet；
3. 让用户选择 sheet；
4. 再按列映射转换成同一种 observation 格式。

SheetJS 被动态加载：只有用户选择 Excel 文件时才下载/解析对应代码，普通 CSV/TSV 启动不承担这部分成本。

### 5.4 数据清洗规则

- 空 cell ID：跳过并警告；
- AP/LR/VD 对应值不是有限数字：跳过并警告；
- 选择了时间/帧但该值无效：跳过并警告；
- 同一个 cell、同一个时间出现多次：保留最后一条有效记录并警告；
- 某些帧没有某个细胞：允许；
- 未知细胞名：仍保留三维位置，只标记 lineage unresolved。

---

## 6. 时间、帧和索引如何工作

所有有效数据最后统一为：

```ts
Observation = {
  cellId,
  step,
  x, y, z,
  renderX, renderY, renderZ,
  parentId?
}
```

无论输入列叫 `time` 还是 `frame`，内部都把用于播放的数值称为 `step`。同时保留 `temporalMode`，因此界面仍会正确显示 Time 或 Frame。

程序预先建立两个重要索引：

```text
frameIndex:
  step → 该时间点的所有 observations

trajectoryIndex:
  cellId → 该细胞按时间排序的所有 observations
```

Python 类比：

```python
frame_index = {t: df[df.time == t] for t in times}
trajectory_index = {
    cell: rows.sort_values("time")
    for cell, rows in df.groupby("cell_name")
}
```

播放时不需要反复扫描完整数据，只需 `frameIndex.get(currentStep)`。轨迹也不需要重新 groupby，只需读取 `trajectoryIndex.get(cellId)`。

播放不插值。时间轴上的每一帧都严格对应源文件中实际存在的数据，因此母细胞和子细胞的出现/消失由输入记录决定。

---

## 7. 坐标归一化原理

原始 AP/LR/VD 数值会完整保留，用于信息框显示。为保持数据层兼容和代码简洁，内部字段仍叫 `x/y/z`，分别固定对应 AP/LR/VD；三维绘制额外使用 `renderX/renderY/renderZ`。

设三轴范围为：

```text
spanX = maxX - minX
spanY = maxY - minY
spanZ = maxZ - minZ
largestSpan = max(spanX, spanY, spanZ)
scale = 16 / largestSpan
```

每个点先减去全局中心，再乘以同一个 scale：

```text
renderX = (x - centerX) × scale
renderY = (y - centerY) × scale
renderZ = (z - centerZ) × scale
```

关键是三轴使用同一个比例，而不是分别拉伸到相同范围。因此原始空间的长宽高比例不会被破坏。

Column Mapper、三维坐标轴和 Cell info 都标为 AP/LR/VD。程序不根据数值自动判断方向，也不会自动翻转正负号；这个生物学含义来自用户在导入时选择的列。给定文件应映射 `A_pos → AP`、`L_pos → LR`、`D_pos → VD`。

---

## 8. 谱系解析为什么不能直接删除最后一个字母

对很多晚期标准命名，子细胞名称看起来像是在母细胞名称后附加 `a/p/l/r/d/v`。但早期谱系存在明显例外，例如：

```text
P0 → AB + P1
P1 → EMS + P2
EMS → MS + E
P2 → C + P3
P3 → D + P4
P4 → Z2 + Z3
```

因此程序从项目内置的 `src/lineage/data/complete_embryo_lineage_list.csv` 读取明确的 `cell_name → parent` 表，而不是删除末尾字符猜 parent。

解析顺序是：

1. 如果用户映射了 parent 列并提供了值，使用用户值；
2. 否则查 canonical parent table；
3. 沿 parent 不断向上，补齐连接数据中细胞所必需的祖先；
4. 如果找不到 parent，该细胞成为 unresolved root；
5. unresolved cell 继续出现在三维和列表中，不导致程序崩溃。

早期 P lineage 的关系还在代码中显式保留了一份可读 safeguard，防止以后更换 canonical 文件时破坏这些关键关系。

### 后代选择

`getDescendants(cellId)` 使用栈进行图遍历：

```text
把起始细胞放入 stack
while stack 非空:
    取出一个细胞
    如果它在数据中 represented，则加入结果
    把它的 children 加入 stack
```

返回的是显式 cell ID 列表。保存 group 时存的也是这些 ID，而不是只存“ABpl lineage”规则，所以分组可以复现。

---

## 9. 谱系树如何绘制

谱系树使用浏览器原生 SVG。每个细胞是一条从出生到分裂或最后观测时间的竖直 `<line>`；分裂时用水平 `<line>` 连接 daughters；名字是 `<text>`。这种画法与经典 C. elegans lineage diagram 一致：时间向下增加，横向主要表达谱系分支而不是空间位置。

布局分两部分：

### 横向位置

- 末端叶节点按 canonical table 的顺序排列；
- 内部细胞放在 daughters 横坐标的平均位置；
- 未知细胞排到独立 root。

### 纵向位置

- 有 Time：细胞出生与分裂高度优先使用上传的 time；
- 有 Frame：优先使用上传的 frame；
- 都没有：直接使用 canonical table 的 developmental minutes；
- 在 Time/Frame 模式中，连接祖先没有观测时，放在最早 daughter 之前一个实际观测步长；
- 单点出现的 terminal cell 至少延伸一个观测步长，保证 branch 可见和可点击。

母细胞的竖线终止于最早 daughter 的出生高度，同一位置绘制水平分裂线，daughter 再从该高度向下延伸。前 6 个层级（depth 0–5）的名字常驻显示；更晚细胞只在鼠标悬停 branch 时显示名字，避免数百个标签遮挡。

D3 Zoom 只负责把 SVG 内容整体平移和缩放。谱系拓扑与布局由本项目代码维护，因此以后可以替换更精确的时间算法，而不影响交互。

当前帧状态由每个细胞的 `firstStep` 和 `lastStep` 判断：

- `currentStep < firstStep`：future；
- `firstStep ≤ currentStep ≤ lastStep`：current；
- `currentStep > lastStep`：past。

整棵树不被隐藏，只通过透明度和虚线保留时间上下文。

---

## 10. 三维胚胎如何绘制

### 10.1 Three.js 与 WebGL

Three.js 是对 WebGL 的高级封装。WebGL 使用 GPU 绘制三维图形，可以类比为 Python 中的 OpenGL/VisPy，但直接运行在浏览器里。

React Three Fiber 让 Three.js 对象可以作为 React 组件组织。Drei 提供 OrbitControls、三维线条和 HTML 标签等常用工具。

### 10.2 为什么使用 InstancedMesh

如果每个细胞核都是一个单独的 Three.js sphere，几百个对象会产生较多 draw calls。项目使用 InstancedMesh：

- 球面几何只保存一份；
- 每个细胞只提供位置矩阵、缩放和颜色；
- GPU 一次批量绘制许多同形状细胞核；
- 帧变化时复用 mesh，只更新 instance buffer。

为了支持不同透明度，当前帧细胞被分为两个 instanced mesh：

- 明亮/选中/着色细胞；
- 灰色或淡化背景细胞。

这比为每个细胞创建独立材质更轻量。

### 10.3 相机交互

OrbitControls 提供：

- 左键拖动：旋转；
- 右键拖动：平移；
- 滚轮：缩放；
- damping：松手后平滑减速；
- Reset：回到固定初始位置；
- Focus：计算当前帧选中细胞的中心并移动相机 target。

相机命令带有只在用户点击 Reset/Focus 时改变的 `nonce`。播放换帧只更新 nuclei buffer，不再次执行旧的相机命令，所以手工旋转、缩放和平移会在播放期间保持。

点击一个 instance 时，Three.js 返回 `instanceId`。程序用它找到对应 observation，然后更新共享 selection，所以列表和树也会同步。

---

## 11. 颜色、分组和显示模式

每个保存的 group 包含：

```ts
{
  id,
  name,
  color,
  cellIds: string[],
  visible,
  source,       // manual、cell、lineage 或 group
  rootCell?,
  createdAt
}
```

颜色显示逻辑集中在 `cellAppearance.ts`，而不是分别写在树、列表和三维组件中。这样同一个细胞在所有视图中使用相同规则。

### Color groups

- 所有细胞可见；
- 着色细胞使用组颜色；
- 其他细胞为灰色。

### Highlight

- 组细胞或当前 selection 明亮不透明；
- 其他细胞仍可见但明显淡化。

### Isolate

- 只显示 visible groups；
- 如果还没有 group，则可显示当前 selection；
- 多个 visible groups 可以同时存在。

一个细胞属于多个可见组时，最近创建的可见组颜色优先。隐藏一个 group 后，如果该细胞还属于另一个可见组，它仍然可见。

---

## 12. 轨迹如何实现

轨迹只针对 selection，不默认绘制所有细胞。

程序从 `trajectoryIndex` 取出每个选中细胞的历史 observation，然后：

1. 删除晚于当前 step 的位置；
2. 按 5、10、25 或 all previous 限制历史窗口；
3. 把归一化位置连接成细线；
4. 使用该细胞或 group 的颜色。

没有对不存在的时间点进行插值，也没有强行把母细胞轨迹连接到子细胞。

---

## 13. Session JSON 是什么

Session 文件只保存轻量配置：

- 原始文件名；
- column mapping；
- cell colors；
- groups 及其显式 cell IDs；
- group 名称和可见性；
- display settings。

它不复制原始数据，所以文件很小，但恢复时必须先加载原数据，再导入 Session JSON。

这种设计类似保存 matplotlib 的绘图参数和 selection，而不是把整个 DataFrame 再保存一次。

---

## 14. 性能设计

当前版本针对“一个或少量已配准胚胎、每个胚胎几百细胞、几百时间点”：

- CSV/TSV 流式解析并按 embryo 过滤；
- 只保留必要列；
- 提前建立 `frameIndex` 和 `trajectoryIndex`；
- 谱系拓扑和布局用 `useMemo` 缓存；
- 三维使用 InstancedMesh；
- 帧变化更新 instance matrix/color buffer；
- 不进行生物位置插值；
- 超过 160 个当前细胞时，标签只显示 selection，避免创建过多 DOM 标签；
- XLSX 解析器动态加载。

给定 `ctr_emb1` 的验证结果是：

```text
20,926 observations
722 unique cells
185 time points
最多 362 nuclei/frame
0 unresolved cells
0 import warnings
```

---

## 15. 错误处理与安全边界

导入时错误不会静默消失：无效行、重复行和 unresolved cell 会显示 warning 或 badge。

数据只在浏览器内存中处理。项目没有上传 API、数据库连接或用户账号系统。Session 导出通过浏览器生成本地 JSON 下载。

需要注意：

- 浏览器刷新会清空尚未导出的 group/config；
- Session 不含原数据；
- XLSX 必须整体读入内存，因此超大表优先转换为 TSV；
- 当前版本不做访问控制或文件加密，本机其他能访问文件的人仍可读取源文件和导出的 Session。

---

## 16. 每个项目文件的作用

下面覆盖本工具创建的所有源码、配置、测试和生成文件。

### 16.1 根目录

| 文件 | 作用 |
|---|---|
| `README.md` | 项目的简洁英文说明、安装、输入格式、主要交互、架构和限制。适合快速查阅。 |
| `docs/开发与实现说明（Python背景版）.md` | 本文。解释技术选择、原理、数据流和每个文件。 |
| `docs/用户使用指南（非编程用户版）.md` | 面向最终用户的逐步操作手册。 |
| `package.json` | 类似 Python 的 `pyproject.toml`/`requirements.txt` 加命令入口；声明依赖以及 `dev/build/test` 等命令。 |
| `package-lock.json` | 锁定所有直接和间接依赖的准确版本及下载来源，保证以后安装结果一致。通常不手工编辑。 |
| `index.html` | 浏览器最初加载的 HTML 骨架，提供 `<div id="root">`，React 把整个应用挂载到这里。 |
| `vite.config.ts` | Vite 开发服务器和 Vitest 测试环境设置。 |
| `tsconfig.json` | TypeScript 顶层项目配置，引用浏览器配置和 Node 工具配置。 |
| `tsconfig.app.json` | 应用源码的 TypeScript 严格检查规则。 |
| `tsconfig.node.json` | Vite 配置等 Node 环境代码的 TypeScript 规则。 |
| `.nvmrc` | 推荐 Node.js 版本 `22.14.0`，供 `nvm use` 读取。 |
| `.gitignore` | 告诉 Git 不跟踪可重新生成的 `node_modules/`、`dist/`、缓存和 `.DS_Store`。 |

### 16.2 `src/` 应用入口

| 文件 | 作用 |
|---|---|
| `src/main.tsx` | 创建 React root，加载全局 CSS，并渲染 `App`。 |
| `src/App.tsx` | 顶层页面：空白欢迎页、加载数据后的四区域布局、文件信息、Session 导入导出和 warning。 |
| `src/styles.css` | 所有界面样式：布局、字体、颜色、按钮、modal、树、列表、播放面板和三维覆盖控件。 |
| `src/vite-env.d.ts` | 告诉 TypeScript 如何理解 Vite 环境及 `?raw` CSV 文本导入。 |

### 16.3 `src/data/` 数据层

| 文件 | 作用 |
|---|---|
| `types.ts` | 定义 ColumnMapping、Observation、EmbryoDataset、bounds 等共享数据结构。 |
| `columnMapping.ts` | 根据常见列名自动建议映射，并检查必填字段和冲突。 |
| `loaders.ts` | 文件预览、CSV/TSV Worker 流式解析、embryo 过滤、XLSX worksheet 读取。 |
| `frameIndex.ts` | 数字检查、无效行处理、重复处理、坐标归一化、frame/trajectory/cell summary 索引。 |
| `embryoView.ts` | 从 active embryo list 生成 overlay observations、逐 cell 平均位置和对应 trajectories。 |
| `loaders.test.ts` | 测试 embryo ID 扫描以及多个被勾选 embryo 的过滤导入。 |
| `embryoView.test.ts` | 测试 overlay、mean position 和多胚胎 trajectory。 |
| `columnMapping.test.ts` | 测试给定数据的 `cell_name/A_pos/L_pos/D_pos/time/embryo_id` 是否能正确自动建议。 |
| `frameIndex.test.ts` | 测试归一化、索引、重复和无效值处理。 |

### 16.4 `src/lineage/` 谱系层

| 文件 | 作用 |
|---|---|
| `canonicalLineage.ts` | 读取同目录 `data/complete_embryo_lineage_list.csv`，建立 canonical cell map，并显式保护 P0–P4 等早期关系。 |
| `data/complete_embryo_lineage_list.csv` | 随工具保存的 canonical cell-parent 数据；构建时直接打包，是谱系解析必需文件。 |
| `lineageResolver.ts` | 合并 canonical parent 与输入 parent override，补齐祖先，建立 children，并提供 ancestors/descendants 查询。 |
| `lineageTree.ts` | 把静态谱系拓扑转换为经典竖直 cell branch、水平 division connector、time ticks 和 SVG 尺寸；处理 observed/canonical 时间。 |
| `lineageResolver.test.ts` | 测试早期特殊关系、后代遍历、unknown cell 和 supplied parent。 |
| `lineageTree.test.ts` | 测试母细胞 branch 是否在 daughter 出生处结束、division connector 是否正确，以及无时间列时是否使用 canonical minutes。 |

### 16.5 `src/state/` 共享状态层

| 文件 | 作用 |
|---|---|
| `explorerStore.ts` | 整个应用唯一的核心状态：dataset、current frame、play/pause、selection、groups、colors、display settings、camera command、Session 导入。 |
| `cellAppearance.ts` | 给定 cell、selection、groups 和 display mode，统一计算颜色、透明度和是否可见。 |
| `explorerStore.test.ts` | 测试 lineage selection、颜色持久化、group 隐藏/改名/改色/删除。 |

### 16.6 `src/components/` 界面组件

| 文件 | 作用 |
|---|---|
| `FileLoader.tsx` | 多文件选择、逐文件 mapping/loading、示例数据、进度/错误，以及合并数据写入 store。 |
| `ColumnMapper.tsx` | 列映射 modal、Time/Frame、worksheet、embryo ID 扫描和多选 checkbox。 |
| `LineageTree.tsx` | 绘制经典 SVG tree、时间状态、branch 持久颜色、D3 zoom/pan，以及 Cell/Lineage/Command-Control 累加选择。 |
| `Embryo3D.tsx` | Three.js instanced nuclei、稳定 OrbitControls 相机、overlay/mean、embryo 着色、轨迹、标签和坐标轴。 |
| `EmbryoPanel.tsx` | 选择同时显示的 embryo list，并切换 Overlay、Mean position 和 Color by embryo。 |
| `FileLoader.test.tsx` | 集成测试多个文件依次 mapping，以及同一文件勾选多个 embryo ID 后的合并导入。 |
| `EmbryoPanel.test.tsx` | 测试显示 embryo 的勾选、Overlay/Mean 切换和 embryo 着色开关。 |
| `CellList.tsx` | 细胞搜索、checkbox 多选、单行 lineage 按钮、palette 着色、是否保存 group。 |
| `GroupPanel.tsx` | group 列表及改名、改色、显示/隐藏、select、focus、delete。 |
| `ListPanel.tsx` | 在 Cells 和 Cell groups 两个 tab 间切换。 |
| `PlaybackControls.tsx` | Play/Pause、上一帧、下一帧、slider、当前值和 0.25×–4×速度。 |
| `DisplayControls.tsx` | Color/Highlight/Isolate、sphere size、background opacity、trail、label 和 axes。 |
| `CellInfo.tsx` | 显示当前 hover/点击细胞的坐标、时间、parent、ancestors 和后代数量。 |
| `ControlDeck.tsx` | 组合 Playback、Display 和 CellInfo 三部分。 |
| `synchronization.test.tsx` | 集成测试：从 tree 选择 lineage 后 list、colors、groups 是否同步，以及 timeline 操作。 |

### 16.7 `src/services/` 与 `src/utils/`

| 文件 | 作用 |
|---|---|
| `src/services/explorerApi.ts` | 为未来指标提供稳定查询接口：group cells、某帧 group positions、cell trajectory、descendants。 |
| `src/utils/format.ts` | 数字和文件大小格式化。 |
| `src/utils/palette.ts` | 12 个克制、可区分的默认颜色。 |
| `src/utils/session.ts` | 下载 Session JSON 和读取/验证 Session 文件。 |

### 16.8 测试与示例

| 文件 | 作用 |
|---|---|
| `src/test/setup.ts` | 给 Vitest 加载 DOM 断言。 |
| `scripts/validate-supplied.ts` | 命令行扫描用户指定路径的 TSV，验证某个 embryo 的真实规模、索引和谱系解析结果；真实数据不需要放入代码仓库。 |
| `public/examples/synthetic_lineage.csv` | 欢迎页“Explore example”使用的小型模拟数据。 |

### 16.9 下载和生成目录

| 目录/文件 | 作用 |
|---|---|
| `node_modules/` | npm 下载的全部运行和开发依赖。体积较大，可通过 `npm install` 重建。不要手工修改。 |
| `node_modules/.tmp/*.tsbuildinfo` | TypeScript 增量构建缓存，可删除、可重建。 |
| `dist/index.html` | 生产构建入口。 |
| `dist/assets/index-*.js` | 压缩后的主应用 JavaScript。文件名 hash 用于缓存失效。 |
| `dist/assets/xlsx-*.js` | 独立、懒加载的 XLSX 解析代码。 |
| `dist/assets/index-*.css` | 压缩后的样式。 |
| `dist/examples/synthetic_lineage.csv` | 构建时复制的示例数据。 |

`dist/` 不是手写源码。每次 `npm run build` 都可能产生新的 hash 文件名。

---

## 17. 主要依赖为什么存在

| 依赖 | 原理/用途 | Python 类比 |
|---|---|---|
| `react`, `react-dom` | 组件化界面和增量 DOM 更新 | Dash/Panel 的组件思想 |
| `typescript` | 静态类型检查 | Python typing + mypy |
| `vite` | 本地开发服务器与前端打包 | Flask dev server + build tool |
| `three` | GPU/WebGL 三维场景 | VisPy/OpenGL |
| `@react-three/fiber` | 用 React 组织 Three.js | 把绘图对象包装成组件 |
| `@react-three/drei` | 相机控制、线、标签等 Three 工具 | 绘图库 helper functions |
| `d3-selection`, `d3-zoom` | SVG selection 和 zoom transform | matplotlib event/transform 工具 |
| `zustand` | 小型全局状态容器 | 一个带订阅机制的共享 dataclass/dict |
| `papaparse` | 浏览器端 CSV/TSV 分块解析 | `csv`/`pandas.read_csv(chunksize=...)` |
| `xlsx` | 浏览器端 Excel workbook 读取 | `openpyxl`/`pandas.read_excel` |
| `lucide-react` | 统一线性图标 | 图标资源库 |
| `vitest` | 单元和组件测试 | pytest |
| `jsdom` | 在测试中模拟浏览器 DOM | 测试用虚拟浏览器页面 |
| `testing-library` | 按用户可见角色点击和断言 | Selenium 风格但更轻量 |

---

## 18. 测试和构建命令在做什么

### 自动测试

```bash
npm test
```

Vitest 会执行 10 个 test 文件、26 个测试，覆盖：

- 给定数据的自动列映射；
- 坐标归一化；
- frame 和 trajectory index；
- 无效行和重复；
- P lineage 特殊关系；
- unknown cell；
- parent override；
- lineage selection；
- tree/list/group/color 同步和 Command/Control 累加选择；
- timeline 前后步进、拖动，以及换帧不发出 camera reset；
- 多文件导入和 embryo ID checkbox；
- 多胚胎 overlay、逐 cell 平均位置、trajectory 和 color-by-embryo 控件。

### 生产构建

```bash
npm run build
```

执行顺序：

1. TypeScript 检查；
2. Vite 转换 React/TSX；
3. tree-shaking 删除未使用代码；
4. 压缩 JavaScript/CSS；
5. 把结果写入 `dist/`。

### 验证真实数据

```bash
npm run validate:sample -- "/absolute/path/to/01_wt_truncated_axis_aligned.tsv"
```

默认验证 `ctr_emb1`。也可以在文件路径后指定其他 embryo：

```bash
npm run validate:sample -- "/absolute/path/to/data.tsv" ctr_emb2
```

用逗号分隔可以同时验证多个 embryo，并额外打印各 embryo observation 数量和最后时间点的平均 nucleus 数量：

```bash
npm run validate:sample -- "/absolute/path/to/data.tsv" ctr_emb1,ctr_emb2
```

它只读取数据并打印统计，不改变原 TSV。

---

## 19. 将来增加定量指标时从哪里开始

不要把计算直接写进 `Embryo3D.tsx`。推荐流程：

1. 在 `src/services/` 或新的 `src/analysis/` 写纯函数；
2. 使用 `createExplorerDataApi` 获取 group cells、frame positions 或 trajectory；
3. 给纯函数写测试；
4. 计算结果再由新的 React component 显示；
5. 如果计算很重，再考虑 Web Worker。

示意：

```ts
const positions = api.getGroupPositions(groupId, frame)
const rg = calculateRadiusOfGyration(positions)
```

这样数据层、计算层和显示层保持分离，未来胚胎注册或统计分析不会破坏当前交互。

---

## 20. 当前版本明确没有实现什么

- 胚胎 registration；
- variability/uncertainty envelope 和经过 registration 的 consensus embryo（当前 Mean 只是对已对齐输入逐 cell 求算术平均）；
- 细胞表面、显微图像、体渲染；
- 位置插值；
- 定量 cohesion/purity/connectedness 指标；
- 自定义谱系编辑；
- 云端保存或多人共享；
- 自动生物轴解释；
- 大规模服务端数据库。

这些是有意留到未来的范围，不是当前代码遗漏。
